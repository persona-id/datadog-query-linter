# This file should not be processed
apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
    iam.gke.io/gcp-service-account: web@web.iam.gserviceaccount.com
  name: web-workflows-service-account
  namespace: web
